<script>
<%#= render 'shared/google_analytics' if Rails.env.production? %>
  var production = <%= raw Rails.env.production?.to_json %>;
<%= yield :js_global %>
<% if content_for? :js_ready %>
  $(document).ready(function() {
  <%= yield :js_ready %>
  });
<% end %>
</script>